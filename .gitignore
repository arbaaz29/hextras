# Local tooling and build outputs — not part of the theme distribution.
_tools/
public/
exampleSite/public/
exampleSite/resources/
.hugo_build.lock
exampleSite/.hugo_build.lock

# Hugo Modules cache (when modules are enabled in the example site).
exampleSite/go.sum

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/
